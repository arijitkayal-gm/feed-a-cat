import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import SessionWrapper from "@/components/SessionWrapper";
import { Toaster } from "sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Feed a Cat",
  description: "Feed a Cat is a fun and heartwarming platform where you can help care for cats by sending virtual or real treats, learning about feline nutrition, and supporting cat rescue initiatives. Every click makes a cat purr!",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased flex flex-col min-h-screen`}
      >
        <SessionWrapper >
          <Navbar />
          <div className="flex-1 text-white bg-neutral-900 bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(120,119,198,0.3),rgba(255,255,255,0))]">
            {children}
            <Toaster richColors closeButton />
          </div>

          <Footer />
        </SessionWrapper>
      </body>
    </html>
  );
}
